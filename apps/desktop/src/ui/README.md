# ui

shadcn-solid components copied in and owned here, over Kobalte primitives.

Copied rather than depended on, so nothing visual is version-locked. Resizable panes
and the file tree are NOT here — those are product, in `panes/`.
