// Compatibility import for consumers that still use the old module name.
// The production route authority lives in desktop-route-manifest.ts.
export {
  DESKTOP_ROUTES,
  type DesktopRoute,
  type DesktopRouteScope,
} from "./desktop-route-manifest";
