# panes

The pane manager: Agent Panes (typed event streams, no PTY), Shell Panes (a real PTY
via xterm.js), the editor pane, and minimize-to-tile.

One webview per window — never two webviews in one window. Detachable panels are
additional Tauri windows running this same app in detached mode.
