# Guardrails batch note

This batch only added the shared `waiting` projection for debug breakpoints (`WaitReason::DebugPaused`). The remaining guardrail configuration, idle, budget, reflection, reassignment, and UI tasks are skipped for the scope-reduction discussion; they need their own implementation batch.
